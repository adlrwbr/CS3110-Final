type wt = string (** TODO *)
type lt = string (** TODO *)
type rt = string (** TODO *)

let add world name category road pos =
    raise (Failure "Unimplemented")

let name world loc =
    raise (Failure "Unimplemented")

let category world loc =
    raise (Failure "Unimplemented")

let locations world =
    raise (Failure "Unimplemented")

let loc_coord loc =
    raise (Failure "Unimplemented")

let roads world =
    raise (Failure "Unimplemented")

let road_coords road =
    raise (Failure "Unimplemented")
